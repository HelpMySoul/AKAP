<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Разрешения -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AKAP"
        tools:targetApi="31"
        android:requestLegacyExternalStorage="true">

        <!-- Активности -->
        <activity
            android:name="activities.TrimAudioActivity"
            android:exported="false" />
        <activity
            android:name="activities.MergeAudioActivity"
            android:exported="false" />
        <activity
            android:name="activities.SplitAudioActivity"
            android:exported="false" />
        <activity
            android:name="activities.ChangeVolumeActivity"
            android:exported="false" />
        <activity
            android:name="activities.ConvertAudioActivity"
            android:exported="false" />
        <activity
            android:name="activities.EditTagsActivity"
            android:exported="false" />


        <activity
            android:name="akap.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>